{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        "uploads": {
           ".read": "true",
           ".write": "auth != null && auth.uid === $uid"
        }
      }
    },
    "resources": {
      ".read": "auth != null",
      "$resourceId": {
        ".write": "auth != null && (!data.exists() || data.child('uploaderId').val() === auth.uid)",
        ".validate": "newData.hasChildren(['title', 'uploaderId'])"
      },
      ".indexOn": ["uploaderId", "subject", "type"]
    },
    "bookmarks": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "comments": {
      "$resourceId": {
        ".read": "auth != null",
        "$commentId": {
          ".write": "auth != null && (!data.exists() || data.child('userId').val() === auth.uid)"
        }
      }
    },
    "notifications": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null"
      }
    },
    "forumPosts": {
       ".read": "auth != null",
       ".write": "auth != null",
       "$postId": {
          ".write": "auth != null && (!data.exists() || data.child('userId').val() === auth.uid)"
       }
    },
    "forumReplies": {
       ".read": "auth != null",
       "$postId": {
          ".write": "auth != null"
       }
    }
  }
}
